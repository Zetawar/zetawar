(ns zetawar.devcards.specs
  (:require
    [devcards.core :as dc :include-macros true]
    [zetawar.game-spec])
  (:require-macros
    [devcards.core :refer [defcard defcard-rg]]))

(defcard map-spec
  {:stub-key :stub-value})

(defcard scenario-spec
  {:stub-key :stub-value})
